# This Dockerfile is intentionally left empty as the actual Dockerfiles are generated dynamically by the update.sh script
# based on the latest releases of Angie. The update.sh script fetches the latest versions and variants of Angie,
# generates the corresponding Dockerfiles, and updates the build configuration accordingly.
FROM scratch
